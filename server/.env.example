DATABASE_URL="postgresql://kapitein:kapetein@localhost:5432/project_tracking?schema=public"
DIRECT_URL="postgresql://kapitein:kapetein@localhost:5432/project_tracking?schema=public"
JWT_SECRET="replace-this-secret"
PORT=4000
CLIENT_ORIGIN="http://localhost:5173"
NODE_ENV="development"

# Supabase Storage (leave empty to write files to server/uploads locally)
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
SUPABASE_STORAGE_BUCKET="uploads"

# Seed (used by `npm run db:seed`)
ADMIN_NAME="Administrator"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-me"
SEED_RESET="false"

# Optional SMTP (falls back to Ethereal if empty)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
RESEND_API_KEY=""
NOVU_SECRET_KEY=""
ANTHROPIC_API_KEY=""

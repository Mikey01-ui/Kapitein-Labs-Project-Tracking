# Same-origin API when hosted on Vercel / nginx. Vite still proxies /api in local dev.
VITE_API_BASE_URL=/api

# Optional Novu inbox (public application identifier)
REACT_APP_NOVU_APPLICATION_IDENTIFIER=
NOVU_BACKEND_URL=
NOVU_SOCKET_URL=

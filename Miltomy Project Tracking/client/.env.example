# Leave as /api when nginx (Docker) or Vite proxy is in front of the API.
VITE_API_BASE_URL=/api

# Optional Novu inbox (public application identifier)
REACT_APP_NOVU_APPLICATION_IDENTIFIER=
NOVU_BACKEND_URL=
NOVU_SOCKET_URL=
